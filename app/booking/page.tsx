import type { Metadata } from "next";
import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { services } from "@/data/services";

export const metadata: Metadata = { title: "Book an Appointment | Kim's Nails", description: "Request an appointment at Kim's Nails in New York. We will confirm your requested service and time shortly." };

export default function BookingPage() { const options = services.flatMap((category) => category.items.map((service) => `${category.category} — ${service.name}`)); return <main><SiteHeader /><section className="booking-page shell"><div><p className="eyebrow">Appointment request</p><h1>Your moment<br />starts <em>here.</em></h1><p>Tell us a little about what you&apos;re dreaming of. We&apos;ll be in touch to confirm the perfect time—and we always welcome your inspo.</p><div className="booking-contact"><span>Prefer to chat?</span><a href="tel:+12125550184">(212) 555–0184</a><a href="mailto:hello@kimsnails.com">hello@kimsnails.com</a></div></div><form className="booking-form" action="mailto:hello@kimsnails.com" method="post" encType="text/plain"><label>Full name<input name="name" required autoComplete="name" /></label><div className="form-row"><label>Phone<input name="phone" type="tel" required autoComplete="tel" /></label><label>Email<input name="email" type="email" required autoComplete="email" /></label></div><label>Service<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{options.map((option) => <option key={option}>{option}</option>)}</select></label><label>Preferred date<input name="preferredDate" type="date" required /></label><label>Anything else we should know?<textarea name="notes" rows={4} placeholder="Inspo, preferred technician, or anything else..." /></label><button className="button button-dark" type="submit">Send request <span className="arrow">↗</span></button><p className="form-note">This opens your email app with your request. We&apos;ll confirm every appointment personally.</p></form></section><SiteFooter /></main>; }
